---
- name: Install common packages
  hosts: all
  become: true
  become_method: su

  tasks:
    - name: Install common packages
      ansible.builtin.dnf:
        name:
          - htop
          - vim
          - wget
          - fastfetch

    - name: Create user jeremy
      ansible.builtin.user:
        name: jeremy

    - name: Copy SSH key to profile
      ansible.builtin.authorized_key:
        user: jeremy
        state: present
        key: "{{ lookup('file', '/Users/jeremy/.ssh/id_ed25519_sk.pub') }}"
